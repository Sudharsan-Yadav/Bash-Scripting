When you are in root user:

#! /bin/bash

if [[ $(whoami) == "root" ]];
then
        echo "You are in root user"
else
        echo "You are not in root user"
fi

Output:
You are in root user

--------------------------------------------------------------------

When you are in ec2-user user:

if [[ $(whoami) == "root" ]];
then
        echo "You are in root user"
else
        echo "You are not in root user"
fi

Output:
You are not in root user
