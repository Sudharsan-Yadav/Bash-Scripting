#! /bin/bash
name=""

if [[ -z $name ]];
then
        echo "It is an empty string"
else
        echo "It is non-empty string"
fi


Output:
It is an empty string

----------------------------------------------------------------

#! /bin/bash
name="Sudhan"

if [[ -z $name ]];
then
        echo "It is an empty string"
else
        echo "It is non-empty string"
fi


Output:
It is non-empty string

----------------------------------------------------------------

#! /bin/bash
name=" "

if [[ -z $name ]];
then
        echo "It is an empty string"
else
        echo "It is non-empty string"
fi


Output:
It is non-empty string
